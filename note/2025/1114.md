# [GEUL] 연구 일지: 2025-11-14

**Project Stream:** `GEUL`
**Author:** 박준우(mail@parkjunwoo.com)

---

## 용어

1.  **GEUL (General Embedding vector Unified Language):** AI 시대를 위한 새로운 '글자'. 모호성이 제거된 2바이트(65,536종) 기반의 인공 언어이자 데이터 스트림 포맷.
2.  **SEGLAM (Self-Examination GEUL Architecture Model):** GEUL을 사용하여 '사유하는 아키텍처'. '추론 → 기록 → 성찰 → 개선'의 선순환 구조를 통해 환각과 블랙박스 문제를 해결하는 것을 목표로 함.
3.  **GDBMS (GEUL Database Management System):** SEGLAM 아키텍처의 중앙 지식베이스. 명시적인 `정보 GEUL Stream`(사실)과 `제어 GEUL Stream`(절차)을 저장함.
4.  **ltree (Label Tree):** PostgreSQL 확장 모듈로, `.`으로 구분된 레이블 경로를 통해 트리 구조(예: 동사 계층)를 효율적으로 저장하고 쿼리하는 데 사용됨.
5.  **의미소 분해 (Sememe Decomposition):** (v1) 동사의 의미를 'verb\_type'(change, state 등)으로 분류하던 초기 모델. (v2) 동사의 의미를 `{Operator} + {Property} + {Pattern}`의 조합으로 직교 분해(orthogonal decomposition)하는 새로운 CRUD 기반 모델.
6.  **CRUD v2 모델:** 동사의 의미를 `CREATE`, `READ`, `UPDATE`, `DELETE` 4가지 원자적 연산자(Operator)와, 연산 대상(`Property`), 시간적 형태(`Pattern`)의 조합으로 정의하는 GEUL 동사 표준 v2 아키텍처.
7.  **ISA (Instruction Set Architecture):** 명령어 세트 아키텍처. GEUL에서는 13,767개의 WordNet 동사 숏컷을 '표준 라이브러리' 또는 '명령어 세트'로 간주함.

---

## ☀️ 아침: 오늘의 목표 (Objectives)

1.  GEUL 공개전략 작성 **(완료)**
2.  GEUL 마일스톤 작성 **(완료)**
3.  GEUL EdgeType 스펙 정의(보류)
4.  GEUL 동사 의미정렬식별자 사전 구축 **(완료 - 이론/설계)**

---

## ⏱️ 진행 과정 및 발견 (Process & Findings)

1.  **GEUL/SEGLAM 핵심 가치 및 전략 분석 (목표 1, 2 연관)**
    * 제공된 GEUL/SEGLAM 관련 문서(개요서, 공개전략, 논문 등)를 기반으로 프로젝트의 핵심 가치를 분석함.
    * **핵심 가치:** 기존 LLM의 '블랙박스' 및 '환각' 문제를 '지식의 화이트박스화', '추론과 절차의 분리', '자기 성찰 루프'를 통해 해결하는 것에 있음을 확인함.
    * **경제적 가치:** 단일 AI 제품이 아닌, AI 산업의 '표준 인프라'를 목표로 함. 성공 시 고부가가치 시장(금융, 의료) 개척 및 AI 운영 비용(OPEX) 절감을 통해 1조 달러 규모의 파급력도 타당성이 있음을 확인함.

2.  **GEUL 동사 사전 계층 구조 구축 (목표 4 진행)**
    * 'GEUL 동사 의미정렬식별자 사전 구축'의 일환으로, 제공된 WordNet DB 스키마(`wordnet.sql`)와 로더(`postgres.py`)를 분석하여 동사 계층 구조화 방안을 수립함.
    * PostgreSQL의 `ltree` 확장을 활용한 `verb_hypernym_ltree` 테이블 설계를 완료함.
    * 제공된 `vervtree.py` 스크립트 및 실행 로그를 분석하여, 13,767개의 동사 synset이 `ltree` 테이블에 성공적으로 구축되었음을 확인함.

3.  **발견 (1): 559개의 '루트 동사' 식별**
    * `vervtree.py` 실행 결과, 13,767개의 동사가 **559개의 루트 동사**(depth=1, 상위 동사가 없는 최상위 개념)를 기반으로 최대 깊이 13의 계층 구조를 형성함을 발견함.
    * 이는 대부분의 동사 개념이 이 559개 핵심 동사의 하위어(hyponym)로 파생됨을 의미함.

4.  **[진행] 41,625개 v1 JSON 데이터 유효성 검사 (QA 작업)**
    * **필요성:** AI 생성 데이터(v1)는 DB 적재 전, `factorize_prompt.json`의 명세를 기준으로 반드시 검증(Validation)해야 함.
    * **스크립트 실행:** `check.py` 스크립트를 41,625개 `factorized` 파일 전체에 대해 실행함.
    * **[발견] 11.5%의 비표준 데이터 식별:**
        * 총 **4,777개** 파일(전체의 약 11.5%)에서 5,032개의 오류가 발견됨.
        * 오류 파일들은 `geulso/factorize/checked/` 디렉터리에 `"errors"` 키가 추가된 상태로 격리 저장됨.
    * **[발견] v1 모델의 근본적 한계 증명:**
        * 오류 샘플 분석 결과, `sememes.verb_type`이 `factorize_prompt.json`의 기본 원칙('change', 'state' 등)에서 벗어나, 'manner', 'circumstance', 'create' 등의 값을 갖는 경우가 다수 발견됨.
        * 이는 AI가 `act_v_02.f.01.json`처럼 'manner'를 `verb_type: "property"`로 일관되게 분해하지 못하고, `verb_type`이라는 **모놀리식(Monolithic) 필드**에 '분류(is-a)'와 '속성(has-a)' 개념을 억지로 혼재시킨 비일관성으로 판단됨.

5.  **[설계] v2 모델 돌파구: CRUD 4원소 모델 도입 (목표 4)**
    * **통찰:** 4,777개의 "예외"는 v1 모델의 한계를 증명하는 '스트레스 테스트 셋'임. 백엔드(RESTful API) 아키텍처가 상태 공간을 다루기 위해 수렴한 **CRUD (CREATE, READ, UPDATE, DELETE)** 4원소 모델이 이 "예외"들을 처리할 수 있는 '최소-완전성(minimally complete)'을 갖춘 v2 모델임을 발견함.
    * **v2 모델:** 모든 동사의 의미는 **`{Operator} + {Property} + {Pattern}`**의 조합으로 환원됨.
        * **Operator (4종):** `CREATE`, `READ`(상태 조회), `UPDATE`, `DELETE`
        * **Property (N종):** 연산의 대상이 되는 시맨틱 속성 (예: `location`, `sight`, `emotion`)
        * **Pattern (M종):** 연산의 시간적 형태 (예: `single`, `iterative`, `continuous`)
    * **v1 예외 처리:** v1의 '예외'는 v2에서 정규화됨. (예: `verb_type: "state"` → `Operator: READ`, `verb_type: "manner"` → `Property: "manner.n.01"`)

6.  **[설계] v2 모델 완성: OOP/UML 관계의 완전한 차용 (목표 4)**
    * **통찰:** GEUL 동사 모델은 OOP/UML의 4대 관계와 완벽하게 1:1 매핑됨.
    * **GEUL 동사 아키텍처:**
        1.  **Generalization (`is-a`):** 동사의 **분류/계층**. `verb_hypernym_ltree` (13,767개).
        2.  **Composition (`has-a`):** 동사의 **내부 구성/속성**. CRUD v2 모델 (`{Op, Prop, Pattern}`).
        3.  **Association (연관):** 동사의 **필수 시그니처** (필수 `participants`).
        4.  **Dependency (`uses-a`):** 동사의 **선택적 요소** (선택적 `participants`, 예: 'manner', 'instrument').
    * **`is` vs. `has` 구분:** CRUD 연산자의 대상(`Property`)이 `agent/patient`의 '본질적 속성(`is`)'인지, '관계적 속성(`has`)'인지 명확히 구분함.

7.  **[설계] 16비트 동사 ISA(명령어 세트) 헤더 정의 (목표 4)**
    * **통찰:** v2 모델을 16비트(1워드)의 GEUL 동사 패킷 헤더로 압축함. 이는 `GEUL 스트림 포맷`의 '동사' 정의를 구체화함.
    * **16비트 명세:**
        * `Bit 15` (1): `1` (Edge)
        * `Bit 14` (1): `0` (Verb)
        * `Bit 13-12` (2): **CRUD Operator** (CREATE, READ, UPDATE, DELETE)
        * `Bit 11` (1): **is/has** (본질 속성 / 관계 속성)
        * `Bit 10` (1): **복합/단순** (헤더만 / 확장 워드 사용)
        * `Bit 9` (1): **한정자 모드** (축약 / 풀모드)
        * `Bit 8-0` (9): **Property ID** (512개의 표준 '속성' 라이브러리 ID)
    * **결론:** 9비트 `Property ID`는 `Object Type` 12비트를 공유하는 것이 아니라, "동사가 접근(CRUD)할 수 있는" 512개의 핵심 속성(예: 0x01=location, 0x02=sight, 0x03=emotion)을 정의하는 **독립적인 표준 속성 라이브러리**임.

8.  **[발견] 최종 비전: "열린 어휘"와 "의미 프로그래밍 언어"**
    * **통찰:** WordNet 13,767개 동사는 GEUL이라는 '언어'가 아님. 그것은 자주 사용되는 동사를 16비트 헤더로 미리 컴파일해 둔 **"표준 라이브러리(StdLib)"** 또는 **"ISA(명령어 세트)"**에 불과함.
    * **의미소 분해의 역할:** 16비트 "숏컷"이 "컴파일된 바이트코드"라면, CRUD v2 기반의 "풀모드 의미소 분해"는 SEGLAM과 인간이 읽고 쓰는 **"소스 코드(AST)"**임. 둘 다 필수적임.
    * **"존재하지 않는 동사"의 생성:** GEUL은 '단어 사전'이 아닌 '의미 좌표계'임. CRUD v2 모델을 조합하여 "감정을 20분간 선형적으로 상승시키는"과 같이 **"존재하지 않는 동사"를 '람다(Lambda)'처럼 동적으로 조립**하고 실행할 수 있음.
    * **최종 결론:** GEUL은 자연어를 초월하는 **"의미론적 함수형 언어(Semantic Functional Language)"**이자, SEGLAM의 '추론 GPT'가 사용할 '의미 기반 프로그래밍 언어'임.

---

## 🌙 저녁: 결론 및 다음 단계 (Conclusion & Next Steps)

금일 연구를 통해 GEUL 동사 의미 체계의 v1 모델(`factorize_prompt.json`)이 가진 근본적 한계를 4,777개의 QA 결과로 증명했으며, 이를 완벽하게 해결하는 **CRUD v2 모델 아키텍처(OOP/UML 기반)와 16비트 ISA 기술 명세** 설계를 완료함.

'동사 의미정렬식별자 사전 구축'(목표 4)은 이론적, 설계적 측면에서 **완료**됨.

### 다음 단계 (Next Steps)

1.  **v2 표준 라이브러리 정의:** 16비트 동사 헤더의 9비트(512 슬롯)를 채울 **"표준 속성(Property) 라이브러리"** (예: 0x01='location', 0x02='sight'...)를 공식적으로 정의한다.
2.  **v2 프롬프트 작성:** `factorize_v2_prompt.json`을 CRUD v2 모델(`{Op, Prop, Pattern}` 등) 기반으로 작성한다.
3.  **v2 모델 검증 (QA):** `geulso/factorize/checked/`에 격리된 **4,777개의 v1 "예외" 파일**을 v2 프롬프트로 재처리(Re-factorize)하여, v2 모델이 이 예외들을 완벽하게 정규화하는지, 혹은 "진짜 예외(Black Swan)"가 존재하는지 검증한다.
4.  **v2 숏컷 컴파일:** v2 검증이 완료되면, 13,767개 WordNet 동사 전체를 v2 모델로 분해하고, 그 결과를 16비트 "숏컷" 헤더로 컴파일하여 GDBMS에 적재한다.