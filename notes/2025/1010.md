# **[GEUL] 연구 일지: 2025-10-10**

**Project Stream:** `GEUL` / `HPA` / `SSHG`
**Author:** 박준우(mail@parkjunwoo.com)

---

## **용어**

1.  **GEUL (General Embedding Unified Language)**: 범용 임베딩 통합 언어
2.  **HPA (Hypothesis-Driven Parsing Algorithm)**: 가설 기반 구문 분석 알고리즘
3.  **SSHG (Semantic Superposition Heterogeneous Graph)**: 의미 중첩 이종 그래프

---

## **☀️ 아침: 오늘의 목표 (Objectives)**

1.  전일 완료된 `factorize` 데이터셋 전체에 대한 `validate.py` 실행 결과를 상세히 검토하고 분석한다.
2.  분석 결과를 바탕으로 데이터 정제 스크립트(`corrected.py`)를 구현하고 실행하여, 유효한 교정 사항들을 원본 데이터에 적용한다.
3.  단일 문장을 넘어 MRS와 HPA를 문단 단위로 처리하기 위한 초기 전략을 구상한다.

---

## **⏱️ 오후: 진행 과정 및 발견 (Process & Findings)**

### **1. Factorize 데이터 검증(`validate.py`) 결과 분석**

* 전일 약 15시간에 걸쳐 실행된 `validate.py`가 완료되어, `geulso/factorize/invalid/` 디렉토리에 총 3,843개의 오류 포함 파일을 생성했음을 확인했다.
* `NO_CANDIDATE`의 비율을 확인하기 위해 `find_no.py` 스크립트를 작성하여 실행했다.
    * 전체 오류 파일(3,843개) 중 **20.2%에 해당하는 777개**의 파일에서 LLM이 `NO_CANDIDATE`를 반환했음을 확인했다.
    * 이는 `find.py`의 후보 생성 로직이 약 20%의 경우에 유효한 후보를 찾아내지 못했음을 의미하며, 향후 **임베딩 기반 검색** 등 후보군 생성 로직의 고도화 필요성을 시사한다.

### **2. 데이터 교정 스크립트 (`corrected.py`) 구현 및 실행**

* `invalid` 디렉토리의 교정 파일들을 읽어, `NO_CANDIDATE`가 아닌 유효한 교정 사항만 `factorized` 원본 파일에 적용하고 `corrected` 디렉토리에 저장하는 `corrected.py`를 작성하여 실행했다.
* 실행 결과, `NO_CANDIDATE`만 포함된 694개 파일을 제외하고 **총 3,149개의 파일이 성공적으로 교정**되었다.
* 총 **3,336건의 개별 오류(Sememe 2,007건, Participant 1,329건)가 자동으로 수정**되었음을 확인했다.
* 무작위 샘플(`accession_v_01.f.00.json`, `stipple_v_02.f.00.json` 등)을 검토한 결과, `corrected.py`가 LLM의 유효한 교정 제안을 원본 데이터에 정확하게 적용했음을 확인했다.
* 교정 안한 개수가 694개로 `find_no.py`의 777개와 일치하지 않아 `corrected.py` 기준이 일치하지 않음을 확인하고, `NO_CANDIDATE`가 하나라도 포함되면 교정하지 않도록 기준을 변경하여 다시 작성하였으나 실행해보지는 못함.

---

## **🌙 저녁: 결론 및 향후 계획 (Conclusion & Next Steps)**

* **결론 1**: `validate.py`와 `corrected.py`로 구성된 **데이터 정제 파이프라인의 1차 실행을 완료**했다. 총 3,843개의 오류 파일 중 3,149개를 자동으로 교정하여, 약 82%의 오류를 해결한 정제 데이터셋 초안을 `corrected` 디렉토리에 확보했다.

* **결론 2**: `NO_CANDIDATE` 비율(20.2%) 분석을 통해, 현재 시스템의 주된 병목 지점은 LLM의 추론 능력이 아니라 **`find.py`의 후보군 생성 능력**임을 명확히 했다.

* **이슈 발견**: `corrected.py`의 교정 제외 기준(694개)과 `find_no.py`의 `NO_CANDIDATE` 포함 파일 수(777개)가 불일치하는 문제를 발견했다. 이는 `corrected.py`가 파일 내에 유효한 교정이 하나라도 있으면 `NO_CANDIDATE`가 포함되어 있어도 교정 대상으로 처리하기 때문이었다. 기준을 강화한 스크립트는 작성했으나 시간 부족으로 재실행 및 검증은 하지 못했다.

* **향후 계획**:
    1.  **데이터 정제 완료**: 강화된 기준의 `corrected.py`를 재실행하여 데이터 정제 작업을 최종적으로 마무리한다.
    2.  **후보군 생성 로직 개선**: `NO_CANDIDATE` 목록을 집중 분석하여, `find.py`에 임베딩 벡터 유사도 검색 기능을 추가하는 등 후보 생성 로직을 개선하는 작업을 최우선 과제로 진행한다.
    3.  **문단 단위 분석 설계**: 정제된 데이터셋을 기반으로, `mrs.py`를 확장하여 상호참조 해결(Coreference Resolution) 등 문단 단위 Pruning 규칙을 적용하는 HPA의 다음 단계 설계를 시작한다.